const numbers = [1, 'two', 3, 'four'];
numbers[0] = 'one';
numbers;
console.log(numbers)

const number = [1, 'two', 3, 'four'];
number.push(5);
number;
console.log(number)

const numbe = [1, 'two', 3, 'four'];
numbe.pop();
numbe;
console.log(numbe)

const num = [1, 'two', 3, 'four'];
num.shift();
num;
console.log(num)